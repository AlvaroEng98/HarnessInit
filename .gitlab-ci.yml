stages:
  - release

goreleaser:
  stage: release
  image: goreleaser/goreleaser:latest
  variables:
    GIT_STRATEGY: clone
    GIT_DEPTH: 0
  script:
    - goreleaser release --clean
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
